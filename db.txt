show databases;
use `project`;

CREATE TABLE `take_medicine`(
	`id` INT AUTO_INCREMENT PRIMARY KEY,
    `time` TIMESTAMP, 
    `state` VARCHAR(10), 
    `auto_finish` VARCHAR(50),
    `pose_img1` BLOB,
    `pose_img2` BLOB,
    `pose_img3` BLOB,
    `pose_img4` BLOB
    );

show tables;
describe `take_medicine`;
SELECT * FROM `take_medicine`;

delete from `take_medicine` where `id` >0;

drop table `take_medicine`;

system cls;